name: branch_protection_rule--subset
on:
  branch_protection_rule:
    types: [created, deleted]
permissions: {}
jobs:
  probe:
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '22'
          token: ''
      - name: Execute source and complete event probe
        env:
          LAB_CASE: branch_protection_rule--subset
          EVENT_FAMILY: branch_protection_rule
          EXPR_EVENT: ${{ toJSON(github.event) }}
          EXPR_REF: ${{ github.ref }}
          EXPR_SHA: ${{ github.sha }}
          EXPR_ACTION: ${{ github.event.action }}
        run: node probe.cjs
